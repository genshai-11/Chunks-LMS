# Resource-agnostic session questions

A Session Question is an internally identified measurement opportunity whose visible sequence number and optional external reference do not make Chunks-LMS the owner of question content. This keeps assessment capture flexible across learning platforms and prevents a future CCI/CVR or resource integration from becoming a dependency of the core progress model.
